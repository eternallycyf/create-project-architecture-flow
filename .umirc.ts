import { defineConfig } from '@umijs/max';
// const routerConfig = require('./src/routes');

export default defineConfig({
  antd: {},
  access: {},
  model: {},
  initialState: {},
  request: {},
  layout: {
    title: '@umijs/max',
  },
  // routes: routerConfig,
  npmClient: 'pnpm',
});
